<article class="flex flex-1 items-center justify-center p-8 relative">
  <div
    class="bg-size-[20px_20px] absolute inset-0 -z-50"
    style="background-image:linear-gradient(var(--color-border) 1px, transparent 1px), linear-gradient(90deg, var(--color-border) 1px, transparent 1px)"
  />

  <slot/>

</article>
